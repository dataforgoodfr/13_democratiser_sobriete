# @package _global_

defaults:
  - _self_
  - data: mobility_dataset # choose datamodule with `test_dataloader()` for evaluation
  - model: bert
  - logger: null
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???

[project]
name = "chat_sufficiency_backend"
version = "0.1.0"
description = "Python backend for Chat Sufficiency application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "pydantic>=2.12.5",
    "uvicorn>=0.40.0",
]
